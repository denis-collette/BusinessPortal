<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:BusinessPortal.ViewModels"
             x:Class="BusinessPortal.Views.PortalPage"
             Title="Services Portal">

    <ContentPage.BindingContext>
        <viewModels:PortalViewModel />
    </ContentPage.BindingContext>

    <ScrollView>
        <VerticalStackLayout Spacing="20" Padding="20">
            <Label 
                Text="Welcome to your Business Portal!"
                FontSize="24"
                FontAttributes="Bold"
                HorizontalOptions="Center" />

            <Label 
                Text="Select a service to get started."
                FontSize="16"
                HorizontalOptions="Center" />

            <CollectionView ItemsSource="{Binding Services}">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Frame Padding="15" CornerRadius="10" HasShadow="True" Margin="0,10">
                            <VerticalStackLayout>
                                <Label Text="{Binding Name}" FontSize="18" FontAttributes="Bold" />
                                <Label Text="{Binding Description}" FontSize="14" />
                            </VerticalStackLayout>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>

